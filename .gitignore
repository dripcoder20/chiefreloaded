# Build output
.task
bin
frontend/dist
frontend/node_modules

# Wails-generated TypeScript bindings
frontend/bindings

# Wails platform build artefacts
build/linux/appimage/build
build/windows/nsis/MicrosoftEdgeWebview2Setup.exe

# Go
*.test
*.out
coverage.*

# Compiled binaries. bin/ is the build output; loopctl is what `go build
# ./cmd/loopctl` drops at the repo root when run without -o.
/loopctl

# chief runtime artefacts. The PRDs themselves (.chief/prds/*/prd.md) and the
# accumulated codebase notes (progress.md) are tracked deliberately — they are
# the project's memory. Everything below is per-machine and large: worktrees
# carry a full node_modules and a built .app, and the agent transcripts run to
# several MB apiece.
.chief/worktrees/
.chief/**/*.log
.chief/usage.json

.DS_Store
